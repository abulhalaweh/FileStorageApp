
export enum PostMethod {
    addFile,
    deleteDelete,
    editUser,
    editProfile,
    changeLogin,
    changePassword
}

export enum GetMethod {
    getAllFiles,
    getUserFiles,
    info
}

export enum ControllerType {
    patient,
    doctor,
    profile,
    manage
}

export enum UserType {
    Patient,
    Doctor,
    Admin
}

export enum Profile {
    username,
    role,
    sub
}
